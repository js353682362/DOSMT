server:
  port: 8888
spring:
  application:
    name: test
  redis:
    database: 0
    host: 192.168.1.147
    port: 6379
    pool:
      #连接池最大连接数(负数表示没有限制)
      max-active: 8
      #连接池最大阻塞时间
      max-wait: 1
      #最大空闲连接
      max-idle: 8
      #最小空闲连接
      min-idle: 0
    #连接超时时间
    timeout: 0
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jsen?useSSL=false&characterEncoding=utf-8&jdbcCompliantTruncation=false
    username: root
    password: root
com:
  jsen:
    name: jiaosen
    password: 123